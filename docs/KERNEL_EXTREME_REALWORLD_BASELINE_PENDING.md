# Extreme Real-World Holdout v3 Baseline

The 10,000-case benchmark has been defined on `benchmark/extreme-realworld-10000-v3`.

This placeholder exists only to keep the baseline result separate from the benchmark definition. Replace it with the measured report after CI executes the fresh holdout against the unchanged Kernel v2 implementation.
