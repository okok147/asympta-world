# Server-only. Never prefix this value with NEXT_PUBLIC_ and never commit a real key.
OPENROUTER_API_KEY=

# Optional public URL used by the static GitHub Pages client to reach the
# server-hosted planner proxy. The default client fallback is the Asympta
# chatgpt.site deployment.
NEXT_PUBLIC_ASYMPTA_AGENT_API_URL=
